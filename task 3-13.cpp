#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	const int a = 7.2;
	const int b = 4.2;
	int x;
	cout << "Input x =";
	cin >> x;
	cout << "size of (float) :" << sizeof(float);
	cout << "\nsize of (x) :" << sizeof(x);
	float y;
	y = sqrt((| a - b * x | ) / lg(x) ^ 3);
	cout << "y =" << y << endl;
	return 0;
}
