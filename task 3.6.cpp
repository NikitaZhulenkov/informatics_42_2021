#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	const int a = 0;
	const int b = 0;
	int x;
	cout << "Input x =";
	cin >> x;
	cout << "size of (float) :" << sizeof(float);
	cout << "\nsize of (x) :" << sizeof(x);
	float y;
	if | x | >= 1
	{
		y = 1, 2 ^ x - x ^ 1, 2;
	}
	if | x | < 1
	{
		y = acos(x);
	}
	cout << "y =" << y << endl;
	return 0;
}
