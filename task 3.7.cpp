#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	const int a = 1,2;
	const int b = 0,48;
	int x;
	cout << "Input x =";
	cin >> x;
	cout << "size of (float) :" << sizeof(float);
	cout << "\nsize of (x) :" << sizeof(x);
	float y;
	y = (b ^ 3 + sin(a * x) ^ 2) / (acos(x * b * x) + e ^ (-x / 2));
	cout << "y = " << y << endl;
	return 0;
}
